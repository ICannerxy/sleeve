<!--components/relam/index.wxml-->
<view class="container">
    <view>
        <image src=""></image>
    </view>
    <block wx:for="{{fences}}" wx:key="{{index}}">
        <s-fence bind:celltap="onCellTap" fence="{{item}}"></s-fence>
    </block>
    <view class="counter-container">
        <!--        -->
    </view>


</view>
